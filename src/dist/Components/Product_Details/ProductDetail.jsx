import React from 'react';

// bas
// makes another change

let ProductDetail = ({product}) => {
    return <h1>I am the product detail module {product.name}</h1>;
};

export default ProductDetail;